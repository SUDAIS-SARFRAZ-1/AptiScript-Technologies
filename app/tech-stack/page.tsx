import type { Metadata } from "next";
import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata: Metadata = {
  title: "Tech Stack",
};

export default function TechStackPage() {
  return <ComingSoon title="Tech Stack" />;
}
